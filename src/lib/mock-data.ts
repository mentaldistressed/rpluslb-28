
import { User, Ticket, Message } from "@/types";

// These mock data arrays are now empty as we're using the database
export const users: User[] = [];
export const tickets: Ticket[] = [];
export const messages: Message[] = [];
